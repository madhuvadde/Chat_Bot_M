{
   "name": "chat_bot_m",
   "module": "index.ts",
   "type": "module",
   "private": true,
   "scripts": {
      "dev": "bun run index.ts",
      "format": "prettier --write .",
      "prepare": "husky",
      "build": "cd packages/server && bun install && bun run prisma generate && cd ../client && npm run build",
      "start": "cd packages/server && bun run prisma generate && bun run start"
   },
   "devDependencies": {
      "@types/bun": "latest",
      "concurrently": "^9.2.1",
      "husky": "^9.1.7",
      "lint-staged": "^16.1.6",
      "prettier": "^3.6.2"
   },
   "peerDependencies": {
      "typescript": "^5"
   },
   "workspaces": [
      "packages/*"
   ]
}
